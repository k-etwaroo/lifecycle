# Terraform (to write)

Resources: resource group, Container Apps environment (VNet-integrated), Container App,
ACR, Key Vault (+ managed identity access), Postgres Flexible Server, Log Analytics.
AWS side: OHALifecycleRole in accounts 095836797281 and 306274225301 with the
workspaces:* least-privilege policy from docs/REQUIREMENTS.md.
